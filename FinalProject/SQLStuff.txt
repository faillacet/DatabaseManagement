CREATE DATABASE nbaproject;
USE nbaproject;
DROP TABLE player;
CREATE TABLE player (
playerID int primary key not null,
fullName varchar(40),
firstName varchar(20),
lastName varchar(20),
isActive bool);
SELECT * FROM player WHERE isActive = 1;

CREATE TABLE team (
teamID int primary key not null,
name varchar(30),
abbreviation varchar(4),
nickname varchar(20),
city varchar(25),
state varchar(25),
yearfounded int);

INSERT INTO player VALUES(1, NULL, NULL, NULL, 0);
select * from player;
select * from team WHERE name = 'Boston Celtics';

select * from team;

CREATE TABLE playerstats (
playerID integer primary key not null,
timeframe varchar(15),
points float,
assists float,
rebounds float,
playerimpactestimate float,
FOREIGN KEY (playerID) REFERENCES player(playerID)
);
drop table playerstats;
select * from playerstats;

CREATE TABLE teamstats (
teamID int primary key not null,
timeframe varchar(15),
wins int,
losses int,
wPCT float,
fgPCT float,
fg3PCT float,
rebounds float,
assists float,
blocks float,
points float,
FOREIGN KEY (teamID) REFERENCES team(teamID)
);

DROP TABLE teamstats;
select * from teamstats;


CREATE TABLE game (
gameID int primary key not null,
teamID int not null,
matchupteamID int not null,
gameDate varchar(15),



FOREIGN KEY (teamID) REFERENCES team(teamID)
);